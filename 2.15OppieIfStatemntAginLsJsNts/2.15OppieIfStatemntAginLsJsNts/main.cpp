#include <iostream>
using namespace std;



int main()
{

	//Create an integer variable x and set its value to 11. Then create another integer variable y and set it to 43
	//Declare two int variables x and y. Initialize them to 11 and 43 respectively.
	//CODE:

	//Use an if statement where x is less than or equal to y. Then show message to screen if condition is true.
	//CODE:

	//Create cout statement that will say that if the condition were false it would print to screen since it is
	//right after the if block (next step). If I alone Y is greater than x.
	//CODE:

	//Try adding a new 'if' block to see what happens.
	//CODE:

}
