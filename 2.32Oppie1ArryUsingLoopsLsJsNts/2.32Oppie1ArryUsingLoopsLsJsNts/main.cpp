#include <iostream>
using namespace std;




int main()
{

	
	//Declare an integer array named adamArray with 9 elements.
	//CODE:

	//Output statement that provides a header "Element - Value" at the top of the array.
	//CODE:

	//Use a for loop that declares an integer x and initializes that variable to 0, control statement
	//where x is less than or equal to 8, Iterate x by 1. This will loop through 9 index locations.
	//CODE:

	//Assign expression that takes the adamArray with index location x (current position) and sets it equal to 99;
	//CODE:

    //Output adamArray (given location of iteration) with each element located at x. The formatting should match header.
   //CODE:

	//Output congratulation statement that displays to screen notifying the program has fully executed the loop/codeblock 
	//And that back in main part of program/next step.
	//CODE:

}