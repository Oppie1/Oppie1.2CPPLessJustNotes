#include <iostream>
#include <fstream>
using namespace std;




int main() {


	//Instantiate an object (here adamFile) of the ofstream (#include <fstream> header) class.
	//CODE:

	//Call the open() function with the parameter "kanopi.txt" on the adamfile object.
	//CODE:

	//Use adamFile object to write a message to the file about Kanopi. 
	//CODE:

	//Use close() function on the adamFile object to finalize the write operation and
	//close the kanopi.txt file ensuring all buffered data is written to disk.
	//CODE:

}