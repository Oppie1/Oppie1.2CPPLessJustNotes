#include <iostream>
using namespace std;





int main()
{

	//Declare a int variable x and initialize it to 1.
	//CODE:

	//Declare an uninitialized int variable number.
	//CODE:

	//Declare an int variable "total" initialized to 0.
	//CODE:

	//Output a sentence to the screen prompting user for a number.
	//CODE:

	//Create a while loop the will continue to run so long as "x" is less than 5.
	//CODE:

	//Input users numbers into program.

	//Assign "total" to an expression that will keep a running tally of the users inputs.
	//CODE:
	
	//Iterate x by 1;
	//CODE:

	//Use if statement that prompts user for another number so that it only asks the same number of
	//times as the while loop. When false it is skipped so user is not prompted again.
	//CODE:

	//Output a sentence to the screen prompting user for another number.
	//CODE:

	//Output a sentence that displays the final total to the screen.
	//CODE:

	return 0;
}
