#include<iostream>
using namespace std;



//Hello world program. Demonstrates printing to screen

int main() {
	//Print "Hello World" to screen on one line.



	//EXTRA CREDIT:
	//Add return 1 after hello world. After return 1 print hello moon with a new line following it.
	//Then 2 cout statements on one line of your choosing. Then run program to see what happens.
	//Then delete the return 1 in the middle of your program to see what happens.
	//If you want to test a codeblock inside your main program (which is like a all encompassing code block
	//with smaller code blocks inside) you can "return 1" in the body of main() {test other codeblocks
	// here with return 1}. Always use "return 0" at end of main - if the program runs then you know you have 
	//"0" errors!



}