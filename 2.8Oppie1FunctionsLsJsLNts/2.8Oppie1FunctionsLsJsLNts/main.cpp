#include<iostream>
using namespace std;


//Create a void function prototype name printSomething.
//CODE:

int main() {

	//Use a function call that tells the function outside of main to execute at this step
	//in program. The information from the function definition will than be printed to the screen.
	//You do not need the data type within main as it is introduced outside of main.
	//CODE:

	return 0;
}

//Define the printSomething() (with no parameters) function here. You will still need the 
//data type "void" here. In the codeblock of the function just have it print something to the screen.
//CODE:

//void just means not to return anything directly to main(). 
//Main will "call" this function and the function will execute and return control to main.
//Key point: Prototype -> Call -> Definition