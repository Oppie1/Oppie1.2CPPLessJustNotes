#include<iostream>
using namespace std;



int main() {


	//Demonstrates modulus (%) operator. Just remainder of division (25/7=3 with 4 remainder).
	//Declare int var "a" and initialize it to an expression using modulus operator (# % #).
	//CODE:

	//Declare int var "b" with regular division so output will be 4.
	//CODE:

	//Declare int "c" and use addition in the assignment so output will be 7.
	//CODE:

	//Declare int "d" with subtraction in assignment so output will be 3.
	//CODE:

	//Declare integer vars "e" and "f".
	//Demonstrate order of operations (PEDMAS) using same numbers but with two different 
	//expressions and their results.
	//CODE:

	//Now explain and see all of the above variables out to screen using cout statements as well as the variables
	//you just declared and initialized to value.

	cout << "Modulus Operator:" << endl;
	cout << "The modulus '%' operator takes divisor divided by numerator, returns remainder of output." << endl;
	//Write a helpful sentence that will display the first operation to the screen.
	//CODE:

	cout << "Regular division" << endl;
	//Write a helpful sentence that will display the next operation to the screen.
	//CODE:

	cout << "Addition" << endl;
	//Write a helpful sentence that will display the next operation to the screen.
	//CODE:

	cout << "Subtraction" << endl;
	//Write a helpful sentence that will display the next operation to the screen.
	//CODE:

	cout << "First example of order of operations" << endl;
	//Write a helpful sentence that will display the next operation to the screen.
	//CODE:

	cout << "Second example of order of operations" << endl;
	//Write a helpful sentence that will display the next operation to the screen.
	//CODE:

	return 0;
}