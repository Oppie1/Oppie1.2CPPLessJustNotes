#include <iostream>
#include "Claudia.h"
using namespace std;

//This tutorial is on destructors which run at the end of a program automatically as objects are destroyed

int main() {


	//Declare an object cv of the Claudia class.
	//CODE:

	//Output statement that say it runs after the constructor but before the destructor but is in main()
	//CODE:

}