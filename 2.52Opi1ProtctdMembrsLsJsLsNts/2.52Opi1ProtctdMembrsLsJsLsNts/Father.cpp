#include <iostream>
#include "Father.h"
#include "Son.h"
using namespace std;

//Create a constructor of the father class that initializes variables "publicVF" ect to numbers.
//CODE:

//Define a void function of the Father class name fatherObjAllAccess with no parameters
//IMPORTANT. The variables and outputs in the execution codeblock are local to this function not to the 
//ones located in the father header. That said you can easily access and output/use all the VF member
//variables you created and initialized in the father constructor.
//CODE:

	//Assign the variable named publicV to 4
	//CODE:

	//Assign the variable named protectedV to 5
	//CODE:

	//Assign the variable named privateV to 6
	//CODE:


//Output 3 statements that state which variable it is (public, protected, private).