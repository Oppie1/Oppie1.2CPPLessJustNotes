#include<iostream>
using namespace std;


int main() {


	//Declare two int variables initialized to 17 and 47.
	//CODE:

	//Use if statement using the less than operator with cout statement in the code block if true.
	//CODE:

	//Use if statement with less than or equal to operator with cout message if true.
	//CODE:

	//Use if statement that is false with message in code block that will not print because if is false.
	//definition will not show to screen. See bonus at bottom.
	//CODE:

	//Use a this value "equals" that value (==). Just use "6" and "6".
	//CODE:

	//Use if condition with "!=" with values 5 and 8 (dont equal each other) cout statement when true.
	//CODE:

	//Use actual number values  5 < 3 in if condition with message in codeblock (will not execute ).
	//CODE:

	//Use actual numbers 3 <= 3 in if condition with message in codeblock (will execute).
	//CODE:
}
