#include <iostream>
#include <cmath>
using namespace std;



int main() {

	cout << "Welcome to the stock market simulator. You have $1000 with an interest rate of 10% " << endl;


	//Declare 3 float variables "total", principle (1000) and rate (10%). 
	//CODE:

	//Declare an int var to track each day. We will initialize it in for loop.

	//Use a for loop to track 10 days one day at a time that calculates the interest on the principle/total
	//during each iteration.
	//CODE:	

		//Given -> This is thr formula to calculate interest on the principle.
		total = principle * pow(1 + rate, day);
		
	//Output a statement that tracks the day we are on that corresponds to the principles value on that day.
	//CODE:

		//Output statement after for loop has fully executed notifying user that the loop finished successfully and now is day 11.
	//CODE:

}