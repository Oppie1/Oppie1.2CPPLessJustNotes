#include <iostream>
using namespace std;




//Declare a template for Adam class (template HEADER),
//CODE:

//Define function template named addTogether with return type Adam, parameters Adam a and Adam b,
//that returns their sum (a + b)
//CODE:

//This function returns sum of those two values back to main.
//CODE:

int main() {

	//Declare double variables x and y, initialize them to 7.65 and 43.54 respectively.
	//Declare float z uninitialized to store result.
	//CODE:

	//Declare int vars c and d, initialized to 3 and 3.14 respectively. Declare int e
	//uninitialized to store result.
	//CODE:

	//Declare int vars f and g, initialized to 6 and 7 respectively. Declare the h
	//uninitialized to store result.
	//CODE:

	//Call the addTogether function template with arguments x and y. ASSIGN result to z.
	//CODE:

	//Call addTogether function template with double arguments c and d. Assign result to e.
	//CODE:

	//Call the addTogether function template with double arguments f and g. Assign the result to h.
	//CODE:

	cout << "Here we add two doubles together using template\n\n";
	//Use cout statement to print z to screen.
	//CODE:

	cout << "-----------------------------------------\n";
	cout << "Adding two ints(note:3.14 was truncated to 3 at declaration(int), so the \n";
	cout << "the template resolves to the int version):\n";
	//Use cout statement to print e to screen.
	//CODE:

	cout << "--------------------------------------------------------\n";
	cout << "Here we add two whole numbers/int together and it uses the template in the same\n";
	cout << "way as when we add two doubles (decimals together):\n" << endl;
	//Use cout statement to print f to screen,
	//CODE:

}