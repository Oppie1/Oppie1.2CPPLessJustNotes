#include <iostream>
#include<cstdlib>
#include <ctime>
using namespace std;



int main()
{

	//Call srand() that seeds the random number generator using time() function.
	//CODE:

	//Output statement that says the following are 9 unique numbers.
	//CODE:
	cout << "The following 9 numbers are unique" << endl;

	//Use a for loop that initializes integer x to 1 and prints 9 random numbers to the screen.
	//CODE:
	
	//Output sentence with the expression where we call the rand() and % 6 then add 1 to compensate for no remainders (0).
	//CODE:

	//Output statement that for loop has fully executed and this is the next step in the program.
	//CODE:

}