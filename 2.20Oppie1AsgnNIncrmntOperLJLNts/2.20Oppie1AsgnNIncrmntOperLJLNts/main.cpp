#include <iostream>
using namespace std;




int main()
{

	//Declare and initialize 3 int variables x, y and z to 20, 30 and 10 respectively.
	//CODE:

	//Example of increment operator "++" (after or before variable in an expression) which means add by 1.
	cout << "Example of increment operator that ++ after x -> x++ so x still = \n";

	//Output x++ to screen to see effect the order of the increment operator has.
	//CODE:

	//Output x to the screen to see what happens after increment step is completed.
	cout << "Now the 'x' has been incremented in operation above x++ and so has increased by one:\n";

	//Output statement that displays just x to the screen to see new value of x.
	//CODE:

	//Output sentence and y to the screen incremented before the variable.
	//CODE:	

	cout << "\n\nThe modulus operator is '%' expression is saying divide by but only give the remainder - here we use a variable 'z'\n";
	cout << "But we can use a value alone (second example)\n";

	//Output modulus "z" by 3 to screen.
	//CODE:

	//Output another line of code where we just use the number 10 % 3 to the screen.
	//CODE:

}
