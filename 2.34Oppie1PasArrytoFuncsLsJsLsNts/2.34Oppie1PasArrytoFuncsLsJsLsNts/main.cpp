#include <iostream>
using namespace std;



//Declare void function called printArray with the parameters of the array itself (int data type), 
//as well as an int variable for the size of the array.
//CODE:

int main() {

	//Declare integer array named adam with 3 elements -> 5, 10, 15
	//CODE:

	//Declare int array named claudia with 6 elements -> 10, 20, 30, 40, 50, 60.
	//CODE:

	cout << "The following is the result of the function printArray with parameters claudia and 6" << endl;

	//Call the printArray function with arguments claudia (for array to pull from), and 6 (which corresponds to index location 5.)
	//CODE:

	cout << "The following is the result of the function printArray with parameters adam and 1" << endl;
	//Call the printArray function on the adam array and the index location of 1. Which will only print out 2 elements.
	//CODE:

}

//Create a void function definition for the printArray function. 

//Use for loop that will iterate through the indices. Initialize x to 0, control x less than sizeOfArray variable
//accounting for each element iterate x by 1. Code block prints location and element value to screen.
//CODE:


//Output statement that will print index number x and then the element itself via theArray variable @ [x]
//CODE:


