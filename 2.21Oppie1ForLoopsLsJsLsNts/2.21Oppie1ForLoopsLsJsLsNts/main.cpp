#include <iostream>
using namespace std;




int main()
{

	
	cout << "Here we use a for loop that will print numbers (5-25). Then will go to next step in program." << endl;

	//Use a for loop that declares and initializes int x and uses less than in control to list numbers 0-25
	//that increments by 5 to do so. Display output to screen during each iteration of the loop.
	//CODE:

	//Out x to screen each iteration.
	//CODE:

	//Output a message to notify user for loop has fully executed (code block) and that they are back in main.
	//CODE:

}