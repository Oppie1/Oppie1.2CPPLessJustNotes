#include <iostream>
using namespace std;



//Declare a new global int variable named tuna then initialize it to 33.
//CODE:

//Call a blank void int function tunaSentence that outputs
//"This is a global variable tuna, it uses a function outside main. Here it is.
//CODE:

int main()
{
	//Declare a local int variable tuna (can be same name as global variable) and initialize it to 20.
	//CODE:

	//Output statement that describes being within main() but accessing global tuna variable using 
	//"::" -> scope resolution operator.
	//CODE:

	//Output statement using the local variable within main to print the tuna variable out to screen.
	//CODE:

	cout << "\n---I am example of function outside main using the global variable int tuna---\n\n" << endl;

	//Call the function outside main.
	//CODE:

}