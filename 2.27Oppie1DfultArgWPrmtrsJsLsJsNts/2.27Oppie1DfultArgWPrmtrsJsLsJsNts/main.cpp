#include <iostream>
using namespace std;




//Declare an int function prototype volume with parameter length(l), width(w), height(h) all initialized to 1.
//CODE:

int main()
{
	cout << "Here is the volume of two cubes via length(l) * width(w) * height(h):" << endl;
	cout << "The volume of the cube with arguments 3, 4, 2 (l*w*h) is:" << endl;

	//Call function with ARGUMENTS that will show the volume out to the screen (l*w*h numbers) and output to screen.
	//CODE:

	cout << "\n\nExample of using the default parameters in the function when argument does not contain any values:" << endl;
	//Call function for volume without parameters and output to screen.
	//CODE:

	cout << "\n*The function prototype had '1' for l, w and h in its prototype so that is what was used for calculation" << endl;
}

//Define int volume(int l = 1, that will return the expression length(L)*height(h) and width(w).
//CODE:
