#include <iostream>
using namespace std;


//Demonstration of variable initialization and basic arithmetic operations

int main() {


	//Declare and initialize 2 int variables a and b to 4 and 3 respectively.
	//CODE:

	//Declare a sum variable and assign it to an expression where you sum the variables you just created together.
	//CODE:

	//Now declare a variable called difference (or answer, or something else, the word is descriptive and for
	//readability only. It does not define anything directly. Assign difference to expression like you did above.
	//CODE:

	//Now display a helpful sentence with and then a variable on a new line to show answer to the screen.
	//CODE:

	//Now do the same for difference as you did for sum above.
	//CODE:

	return 0;

}
