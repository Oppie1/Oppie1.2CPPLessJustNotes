#include <iostream>
using namespace std;




int main()
{

	//Declare an int var named page and initialize it to 0.
	//CODE:

	//Use a while loop where the integer you just created (page) is less than 20.
	//CODE:

	//Create a cout statement that shows page number during each iteration of the while loop. Tracks current "page is:" -> page
	//CODE:

	//Create a expression that will iterate the page by 3 after each cout statement in the while loop. 
	//CODE:

	//Make a cout statement that states that when seen the "while loop" has fully iterated/executed and is the next step.
	//CODE:
}