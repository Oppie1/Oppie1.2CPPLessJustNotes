#include <iostream>
#include <string>
using namespace std;




//Create a class named codeRocks.
//CODE:

	//Create a public access specifier:
	//CODE:


	//Create a setter function setName() that takes string parameter x and set value x equal to a variable that will 
	//store information from main. Make it descriptive like "welcomeStatement". It should be a void that can be called on, 
	//does action outside main, then returns control to main.
	//CODE:
	

	//Now create a getName() string function that will return a value (here whatever is stored in "welcomeStatement" 
	//variable back to main when called.
	//CODE:


	//Create a private access specifier.
	//CODE:


	//Create the string variable ("welcomeStatement") that will store sentence/string from main.
	//CODE:


int main() {

	//Create an object(object1) of the codeRocksClass.
	//CODE:

	//Now call on that object a welcome statement/sentence/string using the setName() function with argument 
	//which will store in the welcomeStatement variable.
	//CODE:

	//Now use a cout statement and object1 you created to retrieve the sentence stored in string via the 
	//getName() function to show that info to the screen.
	//CODE:



}