#include <iostream>
using namespace std;




int main() {

	//Declare an uninitialized int variable age.
	//CODE:
	int age;

	//Declare an int var named tokens initialized to 47.
	//CODE:

	cout << "\nYou must be at least be 29 years old or have 40 or more tokens to enter online tradeshow.\n" << endl;

	//Output statement that prompts user to enter an age.
	//CODE:
	cout << "Please enter your age: \n" << endl;

	//Input user's info and store it in age variable.
	//CODE:

	//Use an if condition that compares age to 33. If the age is greater than 33 the person can enter the tradeshow. 
	//Then use the "or" (||) operator and another condition where tokens is greater than or equal to 40.
	//CODE:

	//Output statement in codeblock that displays to the screen that user is allowed in the online tradeshow.
	//CODE:


	//Use an else statement to output to screen user could not enter tradeshow because are or tokens.
	//CODE:	

	//CODE:
	cout << "Either if or else/if executed the program goes to next step in main() which is just this cout statement." << endl;
}